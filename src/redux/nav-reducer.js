const initialState = {
    menuURLs: ['profile', 'dialogs', 'news', 'music', 'users', 'settings'],
}

const navReducer = (state = initialState, action) => {
    return state;
}

export default navReducer;